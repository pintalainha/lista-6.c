// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int numeros[10];
   int pos
   for (pos = 0; pos < 10; pos++){
       printf("Digite o numero da posição %d: ", pos);
       scanf("%D", &numero[pos]);
    for (pos = 0 <10; pos ++){
        printf("digite a numero da posição %s: ", pos);
        
    }
   }
    return 0;
}
